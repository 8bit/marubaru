<%= form_for [@type, @thing] do |f| %>
  <% if @thing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thing.errors.count, "error") %> prohibited this thing from being saved:</h2>

      <ul>
      <% @thing.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit "Yup, that's what '#{@thing.name}' is.", class: "btn btn-warning" %>
    <%= link_to 'Nevermind', @thing, confirm: 'Are you sure?', method: :delete, class: "btn btn-warning", style: "margin-left:10px;"%>
  </div>
<% end %>
